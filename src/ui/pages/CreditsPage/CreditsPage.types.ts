import { Point } from "pixi.js";

export interface CreditsPageTypes {
    background: string,
    backCTA: {
        background: string,
        backgroundHover: string,
        position: Point,
    },
}